# Build Validation

Validated on July 16, 2026 with Node.js 22.16.0.

Commands completed successfully:

```bash
npm install
npm run typecheck
npm run build
```

Pinned starter versions:

- Next.js 16.2.10
- React / React DOM 19.2.7
- Zod 4.4.3
- TypeScript 5.8.3 (used because it is compatible with the Next.js build worker)

Generated `.next` output and `node_modules` are intentionally excluded from the archive. Run `npm install` after extracting.
